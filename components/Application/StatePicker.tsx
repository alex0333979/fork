import React from 'react';
import { FormField } from '@/generated/graphql';

type StatePickerProps = {
  formField: FormField
}

const StatePicker: React.FC<StatePickerProps> = ({ formField }) => {
  return (
    <></>
  )
}

export default StatePicker;